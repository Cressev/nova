# CURRENT.md

## 当前任务

Nova 已进入代码实现阶段。当前已修正为对话式 Web Agent 网关，并接入 BigModel GLM-4.7 Provider（通过 `BIGMODEL_API_KEY` 环境变量读取 key）。

## 当前状态

- 已通过 `gh api` 读取到 `Cressev/project-memory-persistence` 的 `SKILL.md` 和 references 模板。
- 已按 Skill 要求创建根目录核心资产：`AGENTS.md`、`CURRENT.md`、`PROGRESS.md`、`log.md`、`TODOList.md`、`user-queries.md`、`findings/`、`reports/`、`references/`。
- 旧记忆目录结构已清理。
- 产品研发文档集已按用户要求使用中文目录结构，并补齐各 durable 目录 README。
- 文档正文已统一为中文。
- 产品研发文档集中的普通文档已清空；README 保留用于说明目录安排和维护规则。
- 已记录用户偏好：只有用户明确要求写某份产品研发文档时，Agent 才能写正文。
- 产品研发文档文件名日期已改为当前初始化日期 `20260603`。
- 已写入 `产品研发文档集/预研文档集/20260603_需求文档.md`。
- 其他普通产品研发文档仍保持空白。
- 已修正技术选型方向：MVP 推荐 Python FastAPI 后端网关 + Web 前端 + 本地 Markdown/JSONL 记忆与 trace；CLI 作为备用启动/诊断功能。
- Langfuse 用于开发者调试、工程观测、trace 分析、prompt/eval 管理；不直接暴露给最终用户。
- 用户看到的任务进度、工具调用、审批、trace 摘要和执行结果，需要在 Web 前端中自行设计。
- 命名偏好已更新：不要使用 `pda` 作为产品名、简称或默认命令名。
- 命名偏好进一步更新：名字要好玩、新颖、有品牌感，避免普通工程缩写。
- 正式产品名已确定为 Nova；默认命令名使用 `nova`。
- 已同步修改需求文档和 v1.0 技术文档，使用 Nova 作为正式产品名。
- v1.0 技术方案已写明：Web-first、本地 FastAPI 网关、Python 后端、用户侧前端可观测、Langfuse 开发调试可观测。
- 用户要求后续产品研发文档优先写飞书云文档，并用 bot 在飞书云盘创建同构目录、导入已写文档、私信通知用户。
- 已使用 `lark-cli` 在用户飞书云盘创建 `Nova/产品研发文档集/` 同构目录。
- 已将已有正文的本地 Markdown 草稿创建为飞书新版文档（docx），Markdown 普通文件不作为后续主要交付形态。
- 已按用户要求删除此前误上传到飞书云盘的 6 个 Markdown 普通文件，只保留 docx 云文档作为主要交付。
- 已使用 bot 私信通知用户初次同步，消息 ID：`om_x100b6ecad9a4a4a4b48a1ffb9fc887b`。
- 已使用 bot 私信通知用户 docx 更正，消息 ID：`om_x100b6ecae7b3b0bcb3c567bb7616f96`。
- 飞书云盘根目录：`https://jcnu7fvwv6c8.feishu.cn/drive/folder/CfHOfw192lym5zdllA6ctINMnPg`。
- 飞书产品研发文档集：`https://jcnu7fvwv6c8.feishu.cn/drive/folder/HZznf3LjmlZ2iYdgPr8cWe4Hncf`。
- 飞书 docx 文档：
  - 产品研发文档集 README：`https://jcnu7fvwv6c8.feishu.cn/docx/XkUVdBFQzojDbWxbXRTcrYcMn6f`
  - 预研文档集 README：`https://jcnu7fvwv6c8.feishu.cn/docx/X7sudxRfdop3pzxzon4c0OilnLf`
  - v1.0 README：`https://jcnu7fvwv6c8.feishu.cn/docx/E6jndpHE6oKiCZxcLJoc6uqTnTh`
  - v1.1 README：`https://jcnu7fvwv6c8.feishu.cn/docx/JULQdy0m7oendJxhIj3cZI93nxh`
  - 20260603_需求文档：`https://jcnu7fvwv6c8.feishu.cn/docx/YybZd1eunoIzrZxDOLEcCx8Rnhb`
  - 20260603_v1.0技术文档：`https://jcnu7fvwv6c8.feishu.cn/docx/CHVSdG6JKolNKRxJGSvceGewn2d`
  - 20260604_Codex_App风格功能开发文档：`https://jcnu7fvwv6c8.feishu.cn/docx/ErprdC3kro4XFhxNObwcDwu7nzh`
- 用户要求先不等审批文档，直接开干、干中学。
- 已创建第一版代码骨架：
  - `src/nova_gateway/`：FastAPI 后端、任务存储、模拟 runtime、trace recorder、CLI。
  - `static/`：本地 Web UI。
  - `tests/`：基础 API 测试。
  - `pyproject.toml`：Python 项目配置。
  - `run_nova.py`：源码直接启动入口。
- 当前本地服务已启动：`http://127.0.0.1:8765`。
- 本轮验证通过：
  - `PYTHONPATH=src python3 -m unittest discover -s tests`
  - `PYTHONPATH=src python3 -m nova_gateway.cli doctor`
  - `GET /api/health`
  - `POST /api/tasks`
  - `GET /api/tasks/{task_id}/events`
  - `GET /api/tasks/{task_id}/trace`
- 已按用户纠正，将 Web UI 从任务面板改为对话式交互。
- 已新增聊天会话和消息接口：`/api/chat/sessions`、`/api/chat/sessions/{session_id}/messages`。
- 已接入 BigModel OpenAI-compatible Chat Completions：
  - base URL：`https://open.bigmodel.cn/api/paas/v4`
  - model：`glm-4.7`
  - key 环境变量：`BIGMODEL_API_KEY`
- 已补充中文代码注释，覆盖 Provider、Store、API 编排和前端关键交互。
- 已初始化 git 仓库并推送到 GitHub private 仓库：
  - 远程仓库：`https://github.com/Cressev/nova`
  - SSH remote：`git@github.com:Cressev/nova.git`
  - main 分支首个提交：`a51a763`
- 用户新增开发交付要求：每次完成开发后必须启动本地网站/服务，并给出可验证 URL。

## 立即下一步

- 增加审批接口和前端审批卡片。
- 实现工作树创建/切换/清理能力。
- 将工具调用事件持久化到 thread 历史，刷新页面后仍能查看执行轨迹。
- 增加命令输出分片、文件 diff 卡片和更完整的异常恢复。
- 等待用户明确指定要写的产品研发文档后，再写对应正文。
- 新增产研文档时使用实际创建日期，不沿用示例日期。
- 如用户继续要求，可按顺序写技术调研、核心功能实现方案或 v1.0 功能开发清单。
- 若用户要求写选型文档，可写入 `产品研发文档集/预研文档集/20260603_技术调研.md`。
- 后续架构文档应体现 Web-first、本地网关、Python 后端、Langfuse 开发者可观测、用户前端可观测五个边界。
- 已确定正式产品名 Nova；后续文档、代码和命令名优先使用 Nova / `nova`。
- 是否立即把项目目录从 `personal-dev-agent` 改为 `nova` 仍待用户确认。
- 飞书同步已完成；后续新增/修改产研文档时，优先创建或更新飞书新版文档（docx），不要用 Drive Markdown 普通文件作为主要交付。

## 已知阻塞

- 初始化偏好尚未完全确认：git 策略、远程仓库、禁止触碰范围、durable artifact 定义、前端是否升级到 React + Vite。
- 产品研发文档正文不得主动补写。
- 当前已接模型 Provider、流式输出、工具元数据、只读工具并行、全局/项目级 Agent 指令注入、项目目录切换和第一版真实工具调用；审批闭环与工作树尚未实现。
- 每次开发收尾必须确认服务已启动并把 URL 交给用户验证。
- 本轮已重做静态 Web UI：深色侧边栏、对话主窗口、模型/网关状态、空状态快捷提示、发送中状态和更清晰的消息气泡。
- 本轮已用用户提供的 BigModel key 启动当前服务进程，key 仅注入运行时环境，不落盘、不提交。
- 当前服务地址：`http://127.0.0.1:8765`。
- Provider 状态已验证：`configured=true`。
- 真实 GLM-4.7 对话已验证，返回内容：`Nova 已连接`。
- 已检查优秀 Skill 的共性：触发条件精准、主文件简洁、复杂内容拆 references、危险操作有门禁、交付前强验证；后续 Nova 的 Skills/工作流按这些原则设计。
- 已安装本地 UI/design 和前端验证相关 Skill：`figma-generate-design`、`figma-implement-design`、`figma-create-design-system-rules`、`figma-use`、`playwright`、`screenshot`。安装后需重启 Codex 才会自动触发，但当前已直接读取其 `SKILL.md` 参考。
- 已实现聊天流式输出：
  - 后端 Provider 支持 OpenAI-compatible SSE。
  - 新增 `/api/chat/sessions/{session_id}/stream` NDJSON 流接口。
  - 前端发送后立即显示用户消息。
  - assistant 消息按 `assistant_delta` 增量更新。
- 当前服务已用 BigModel key 启动，网站地址：`http://127.0.0.1:8765`。
- 流式验证通过：真实请求返回 `user_message`、`assistant_delta`、`assistant_done`。
- 已二次补强流式前端体验：
  - 发送后立即插入用户消息和 Nova 生成中气泡。
  - 前端状态栏展示“正在连接模型 / Nova 正在输出 / 回复完成 / 请求失败”。
  - 流式错误会直接显示为对话消息，不再静默失败。
  - 输入框支持自适应高度，发送完成后自动清空。
- 已参考本地 UI/design skill 重做视觉系统：
  - `figma-implement-design`：按设计系统、视觉验证、响应式和可访问性思路约束实现。
  - `playwright`：用真实浏览器打开、交互、截图和检查控制台。
  - 本轮最终截图：`output/playwright/nova-chat-ui-after-stream.png`。
- 已修复 `/favicon.ico` 返回 404 的问题，现在返回 204，避免浏览器控制台噪音。
- 本轮 Playwright 验证通过：页面打开成功、真实发送短消息成功、无 console error、网络请求均为 200。
- 已按用户要求调研 Codex App 官方手册并写入 v1.0 功能开发文档：
  - 本地文档：`产品研发文档集/v1.0/20260604_Codex_App风格功能开发文档.md`
  - 飞书文档：`https://jcnu7fvwv6c8.feishu.cn/docx/ErprdC3kro4XFhxNObwcDwu7nzh`
  - bot 私信通知消息 ID：`om_x100b6d27586a9c88c4942d2b6ae1113`
- 已将 UI 从双栏聊天页升级为 Codex-style 三栏工作台：
  - 左栏：项目、运行模式、本地/工作树/云端、线程列表、Skills 提示。
  - 中栏：线程状态、计划提示、消息流、slash command 提示、流式 Composer。
  - 右栏：Workspace、Review、Run、Permissions、Browser 占位。
- 已新增只读工作区状态接口：`GET /api/workspace/status`，返回项目根路径、Git 分支、变更文件、运行模式、权限摘要和常用命令。
- 已验证真实 GLM-4.7 在三栏工作台中可流式回复，返回内容：`Nova Codex-style 工作台正常`。
- 本轮截图：
  - 桌面：`output/playwright/nova-codex-style-desktop.png`
  - 移动：`output/playwright/nova-codex-style-mobile.png`
- 已下载并研究 OpenAI 开源 Codex 仓库：
  - 本地路径：`references/upstream/openai-codex/`
  - 当前 clone commit：`ad2012d`
  - 许可证：Apache-2.0
  - 该目录已加入 `.gitignore`，不提交上游源码。
- 用户指定 `references/upstream/src` 后续简称为“cc源码”；Nova 后续设计和实现除参考 OpenAI Codex 外，也必须参考 cc源码的 agent runtime、工具执行、权限、状态线、技能、记忆、MCP 和会话恢复设计。
- 已完成 cc源码第一轮结构复查，稳定结论已沉淀到 `findings/2026-06-05-cc-source-reference.md`。
- 已使用 Superpowers 技能重新审视产品形态，用户确认下一阶段选择 Runtime-first Coding Agent 路线：Nova 要作为“开源求职作品 + 极致自用体验”的 Web-first 个人本地 Coding Agent Runtime，而不是继续堆 UI 面板或回到招聘信息抓取方向。规格文档：`docs/superpowers/specs/2026-06-06-nova-runtime-first-product-design.md`。
- 已按用户要求把“Nova 下一步开发计划与长期路线”写成飞书新版文档，并创建到已确认正确的 `Nova/产品研发文档集` 云盘文件夹；文档链接：`https://jcnu7fvwv6c8.feishu.cn/docx/D5ChdLX3DoTyCbxdCMzcUZkDn3g`。已用 bot 私信通知用户，消息 ID：`om_x100b6d77fb91aca0c2bb2f4ea9848e9`。因 user 身份缺少 `search:docs:read` 和 `space:document:retrieve`，本次未能枚举并放入 v1.0 子目录。
- 用户随后反馈文档不存在；已通过飞书 API 核验文档存在、正文可读、当前授权 user 具备 view 权限，并用纯文本重新发送链接。重新发送私信消息 ID：`om_x100b6d778f47f8a4c146f573fa2094b`。若用户仍无法打开，应优先检查浏览器登录账号/租户是否与当前授权 user 一致。
- 已根据 `openai/codex` 的 turn loop、tool registry、shell/apply_patch handler 和 app-server event schema，实现 Nova 第一版 Codex-like Agent 能力：
  - Python 后端 `CodexLikeAgentRuntime`。
  - 工具注册和工作区边界：`read_file`、`list_files`、`search_text`、`shell_command`、`replace_in_file`、`create_file`、`git_status`。
  - 对话流中展示 `tool_start` / `tool_done` 工具事件。
  - GLM 工具调用标签支持缺失 `</tool_call>` 的容错解析。
  - 前端工具卡片排在最终回答前，并在当前轮完成后保留。
- 本轮验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests` 通过，13 tests。
  - `node --check static/app.js` 通过。
  - `GET /api/health` 返回 `ok=true`。
  - `GET /api/provider` 返回 `configured=true`。
  - Playwright 真实页面验证：发送“请读取 README.md 的前几行，并告诉我项目名称。”后，页面显示 `read_file` 工具卡片和最终回答“项目名称是 Nova”。
  - 控制台错误数 0。
  - 截图：`output/playwright/nova-agent-tool-card-final.png`。
- 本轮按用户批评继续补齐基础闭环：
  - UI 字号和间距已压小，右侧面板从 Browser 占位改为真实 Tools / Memory / Config。
  - 新增 `/api/runtime/config`、`/api/tools`、`/api/memory/status`。
  - 配置支持环境变量：`BIGMODEL_BASE_URL`、`BIGMODEL_MODEL`、`NOVA_PERMISSION_MODE`、`NOVA_NETWORK_ACCESS`、`NOVA_MAX_TOOL_ROUNDS`。
  - 权限模式支持 `read_only`、`ask`、`workspace_write`；`ask` 目前会拦截写入和 shell，等待审批 UI。
  - 工具清单扩展到 9 个：`read_file`、`list_files`、`search_text`、`git_status`、`git_diff`、`shell_command`、`replace_in_file`、`create_file`、`apply_patch`。
  - 只读工具支持并行执行路径；写工具和 shell 不并行。
  - 内置指令支持 `/status`、`/tools`、`/permissions`、`/memory`、`/review`、`/plan`、`/help`。
  - 输入框输入 `/` 会自动弹出可用内置指令。
  - 项目记忆已校正为仿 Codex 的全局 + 项目级指令模型：产品内 Agent 只注入全局 `~/.nova/AGENTS.md` 和当前工作区 `AGENTS.md`。
  - `CURRENT.md`、`PROGRESS.md`、`TODOList.md`、`log.md` 只给 Nova 外层开发过程使用，不注入产品内 Agent。
  - 已新增项目目录切换能力：`GET /api/workspaces` 返回候选目录，`POST /api/workspace/select` 切换当前工作区，并拒绝允许根目录之外的路径。
  - 已修复 `/memory` 内置指令按新记忆分组输出。
  - 已优化 `/` 指令菜单：紧凑结构化列表，Enter 选择首个匹配指令，Ctrl+Enter 发送。
  - Agent 流增加 `agent_status` 状态事件；后端增加通用异常兜底。
- 本轮仍未完成：
  - 前端审批确认 UI。
  - 工作树真实创建/切换。
  - 会话未按项目目录隔离。
  - 工具调用事件刷新后持久展示。
  - 命令 stdout/stderr 分片流式展示。
- 本轮最新验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests` 通过，21 tests。
  - `node --check static/app.js` 通过。
  - Python 编译检查通过。
  - `/api/health`、`/api/provider`、`/api/workspaces`、`/api/memory/status` 通过。
  - 工作区可切到 `/mnt/d/documents/study/code/codex/zotero-note-md`，`/tmp` 被拒绝，已切回 Nova。
  - Playwright 验证右侧 Memory 分组、左侧项目切换、`/` 指令菜单和 Enter 选择行为。
  - 截图：`output/playwright/nova-workspace-memory-slash-final.png`。

## 最后更新

2026-06-04
