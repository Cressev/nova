# Nova 对齐 DSH 验收命令清单

## 本地代码门禁

在项目根目录执行：

```bash
PYTHONPATH=src /Users/liam/.miniforge3/envs/claude/bin/python -m unittest discover -s tests
cd frontend && npx tsc -b && npm run build
cd .. && node tests/frontend_workspace_view.test.js
node tests/frontend_react_smoke.test.js
git diff --check
```

成功状态：后端 `300 tests OK`，TypeScript/build/smoke/Workspace 18 用例/diff check 全通过。

## 用户重启 Nova 后的实时路由门禁

```bash
curl -sS http://127.0.0.1:8765/openapi.json > /tmp/nova-openapi.json
python - <<'PY'
import json
p = json.load(open('/tmp/nova-openapi.json'))
required = [
    '/api/workspaces/rename',
    '/api/workspaces/delete',
    '/api/workspaces/reorder',
    '/api/chat/sessions/reorder',
]
missing = [path for path in required if path not in p.get('paths', {})]
assert not missing, missing
print('实时新路由全部存在')
PY
```

## 实时 UI 门禁

打开：

```text
http://127.0.0.1:8765
```

逐项确认：

1. 搜索标题和正文，结果有 snippet，归档会话不出现；
2. Sidebar passive 轮询不会切换当前 Workspace；
3. Workspace 三点菜单可以重命名、删除确认；
4. Workspace 拖拽顺序刷新后保持；
5. Session 拖拽顺序刷新后保持；
6. rail/wide 切换刷新后保持；
7. pending、running、子会话运行中、已完成未查看、idle 状态文案正确；
8. tree/treeitem 和 `aria-selected` 存在；
9. reduced-motion 下无持续动画。

## 当前未通过原因

截至 2026-09-22 13:46（Asia/Shanghai），实时 `/openapi.json` 是旧宿主快照，四个新路由尚未出现。代码和 TestClient 应用 OpenAPI 已通过，但需要用户重启当前 Nova 宿主后重新执行实时门禁。不得启动替代服务冒充当前 GUI 已更新。

## 交付边界审计

本轮工作树中与本对齐任务直接相关的变更包括：

- `src/nova/api/chat.py`、`src/nova/api/workspace.py`、`src/nova/api/routes.py`；
- `src/nova/models.py`、`src/nova/sessions/store.py`、`src/nova/workspace/manager.py`；
- `frontend/src/App.tsx`、`frontend/src/app.css`、`frontend/src/lib/`；
- `tests/test_api.py`、`tests/test_workspace.py`、`tests/frontend_workspace_view.test.js`；
- 两份差异/验收文档及 `reports/README.md`；
- 构建生成的 Vite hash 产物。

既有 `static` 删除/修改项未做清理或回滚，避免覆盖用户已有工作树变更。未执行提交、推送或宿主重启。

